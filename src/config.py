"""Configuration constants.

NOTE: Default keys below are provided for convenience. If this repository
becomes public, consider removing or rotating them to avoid abuse. Users can
still override by entering their own key in the sidebar.
"""

# Default OpenRouter API key fallback (used only if user leaves the input blank)
DEFAULT_OPENROUTER_KEY = ""

# Default Kimi (Moonshot) API key fallback (used only if user leaves the input blank)
# API: https://platform.moonshot.cn/docs/api/chat
DEFAULT_KIMI_KEY = ""
