"""Configuration constants.

NOTE: The OpenRouter default key below is provided for convenience. If this
repository becomes public, consider removing or rotating this key to avoid
abuse. Users can still override it by entering their own key in the sidebar.
"""

# Default OpenRouter API key fallback (used only if user leaves the input blank)
DEFAULT_OPENROUTER_KEY = ""
